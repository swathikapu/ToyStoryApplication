@model ToyStoryApplication.Models.Franchise

<td>
    <img src="@Url.Content(Model.Logo)" width="70" height="70" />
</td>
<td>
    @Html.DisplayFor(modelItem => Model.Name)
</td>
<td>
    @Html.DisplayFor(modelItem => Model.Origin)
</td>
<td>
    @Html.DisplayFor(modelItem => Model.Type)
</td>
<td>
    @Html.DisplayFor(modelItem => Model.FirstAppearance)
</td>
<td>
    @Html.DisplayFor(modelItem => Model.CreatedBy)
</td>
<td data-sort="@Model.Count">
    @using (Ajax.BeginForm(
        actionName:"Like",
        controllerName: "Franchises",
        routeValues: new {id = Model.Id},
        ajaxOptions: new AjaxOptions
        {
            UpdateTargetId = Model.Id.ToString(),
            InsertionMode = InsertionMode.Replace
        }
     ))
    {
        <input type="submit" value=@Model.Count.ToString() id="likeButton" class="btn btn-primary btn-sm"/>
    }
</td>

<td>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-primary btn-sm editButton" })
    @Html.ActionLink("Details", "Details", new { id = Model.Id }, new { @class = "btn btn-primary btn-sm detailsButton" })
    @Html.ActionLink("Delete", "Delete", new { id = Model.Id }, new { @class = "btn btn-primary btn-sm deleteButton" })
</td>